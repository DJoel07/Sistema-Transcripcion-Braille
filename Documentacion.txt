## 🏗️ Diseño Arquitectónico de Alto Nivel (Python con Flask)

Propondremos una **Arquitectura de Tres Capas (3-Tier Architecture)**, que es ideal para aplicaciones web robustas y mantiene la separación de responsabilidades (*Separation of Concerns*).

| Capa | Componente Principal | Tecnologías | Responsabilidad |
| :--- | :--- | :--- | :--- |
| **1. Capa de Presentación (Frontend)** | Interfaz de Usuario (UI) | HTML, CSS, JavaScript | Maneja la entrada del usuario (texto a transcribir) y presenta los resultados de la transcripción y las opciones de descarga de señalética. |
| **2. Capa de Lógica de Negocio (Backend/Flask Core)** | Servidor de Aplicaciones (Flask) | Python, Flask | Contiene la lógica central del proyecto: **la transcripción de español a Braille y la lógica de generación de archivos de señalética**. |
| **3. Capa de Datos (Data/Persistencia)** | Almacenamiento de Mapeos | Archivos de Configuración (`.json`, `.py`) o Bases de Datos (SQLite/PostgreSQL) | Almacena de manera estructurada los mapeos Braille (abecedario, números, signos), permitiendo una fácil actualización y consulta. |

### Diagrama Conceptual de Flujo

1.  El **Usuario** introduce un texto y presiona "Transcribir" en la **Capa de Presentación**.
2.  La **Capa de Lógica de Negocio (Flask)** recibe el texto.
3.  La **Capa de Lógica de Negocio** consulta la **Capa de Datos** para obtener las reglas de mapeo Braille.
4.  La **Capa de Lógica de Negocio** aplica las reglas para realizar la transcripción.
5.  Si se solicita **Señalética**, la **Capa de Lógica de Negocio** usa librerías de Python (ej. ReportLab) para generar un archivo PDF o SVG.
6.  La **Capa de Lógica de Negocio** devuelve el texto Braille (y/o el archivo descargable) a la **Capa de Presentación**.

---

## 🚦 Documentación del Ambiente y Estrategia de Ramificación (Git)

### 1. Ambiente de Desarrollo

| Componente | Herramienta Seleccionada | Justificación |
| :--- | :--- | :--- |
| **Lenguaje de Programación** | Python 3.x | Sencillez, legibilidad y excelente soporte para procesamiento de texto. |
| **Framework Web** | Flask | Micro-framework ligero y minimalista, fácil de configurar y escalar para el alcance del proyecto. |
| **Manejo de Dependencias** | `pip` y `virtualenv` | Aísla las dependencias del proyecto del sistema operativo (Buenas Prácticas). |
| **Generación de Señalética** | `ReportLab` (Python Library) | Librería especializada en Python para generar documentos PDF de alta precisión (vectoriales), ideal para señalética de calidad de impresión. |
| **Documentación Técnica** | *Docstrings* de Python | Se usarán para comentar el código, que luego puede ser procesado por herramientas como Sphinx para generar documentación tipo Javadoc. |

### 2. Estrategia de Ramificación (Feature Branch Workflow Modificado)

Esta estrategia cumple con el requisito de **no desarrollar en `main`** y separa claramente el trabajo en desarrollo del trabajo de documentación.

| Rama | Propósito | Restricciones |
| :--- | :--- | :--- |
| **`main`** | **Versión Estable.** Solo recibe código probado y listo para producción/entrega. | **No se permite el desarrollo directo (commits)**. Solo se actualiza mediante *merges* (fusiones) desde `develop`. |
| **`develop`** | **Rama de Integración.** Acumula todas las funcionalidades terminadas y probadas antes de liberarlas. | Punto de partida para las ramas de funcionalidad (`feature/`). |
| **`documentacion`** | **Documentos no-código.** Contiene todos los archivos obligatorios de documentación (`.pdf`, `.md`, manuales, etc.). | Se mantiene separada de `develop` para evitar conflictos en el código fuente. Se trabaja y se revisa independientemente. |
| **`feature/nombre-funcionalidad`** | **Desarrollo de Funcionalidades.** Implementación de requisitos específicos (ej. `feature/mapeo-numeros`). | Se crean a partir de `develop` y se fusionan de nuevo en `develop` una vez terminadas y probadas. |

### Flujo de Trabajo (Ejemplo de Transcripción)

1.  **Iniciar Tarea:** Crea una rama de funcionalidad desde `develop`:
    `git checkout develop`
    `git checkout -b feature/mapeo-primera-serie`

2.  **Desarrollar y Probar:** Implementa la lógica de mapeo para la primera serie (a-j).
    `git commit -m "feat: Implementa mapeo de a-j"`

3.  **Integrar:** Cuando esté listo y las pruebas pasen, fusiona de nuevo a `develop`:
    `git checkout develop`
    `git merge feature/mapeo-primera-serie`
    `git push origin develop`

4.  **Liberación (entrega de la iteración):** Una vez que `develop` esté estable y cumpla con la entrega bimestral, se fusiona a `main`:
    `git checkout main`
    `git merge develop`
    `git push origin main`

5.  **Documentación:** El trabajo en la rama `documentacion` se realiza en paralelo. Cada vez que se actualice un manual o la arquitectura, se hace un *commit* en esa rama y se sube:
    `git checkout documentacion`
    `git commit -m "docs: Se actualiza el Manual de Usuario"`
    `git push origin documentacion`